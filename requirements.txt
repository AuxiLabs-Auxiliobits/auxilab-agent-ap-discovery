# ============================================================
# AP Process Discovery Agent — standalone tool dependencies
# Install:  pip install -r requirements.txt
# ============================================================

# --- Core pipeline ---
langgraph==1.2.1
langchain-core==1.4.0
pydantic==2.13.4
python-dotenv==1.2.2

# --- User interface ---
gradio>=5.0.0

# --- LLM providers (either one is enough; both are lightweight to install) ---
google-genai==1.75.0        # LLM_PROVIDER=gemini
anthropic==0.39.0           # LLM_PROVIDER=anthropic

# --- Document input (.docx / .pdf) ---
python-docx==1.2.0
pypdf==5.1.0
